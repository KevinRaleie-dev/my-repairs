import React from 'react'
import { Layout } from '../components/Layout'

const Messages = () => {
  return (
    <div>
        <Layout
        rightElement={() => null}
        >
            <div>
                this is where the messages component is going to be which is gonna display the messages
            </div>
        </Layout>
    </div>
  )
}

export default Messages